# This repository currently has no required runtime environment variables.
# Keep this file as the future contract if download links or site metadata
# need to move from source code into environment configuration.

# Example reserved variables:
# VITE_SITE_URL=https://www.zenmind.cc
# VITE_ANDROID_DOWNLOAD_URL=
# VITE_IOS_APP_STORE_URL=
